{
  "name": "fitness-tracker",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:client": "npm --prefix cilent run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix cilent run build",
    "lint": "npm --prefix cilent run lint",
    "preview": "npm --prefix cilent run preview"
  },
  "dependencies": {
    "react-router-dom": "^7.13.1"
  }
}
